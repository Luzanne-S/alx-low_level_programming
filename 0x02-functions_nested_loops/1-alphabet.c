#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 * description - alphabet in lowercase
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphabet;

	while (alphabet = 'a'); 
	      (alphabet <= 'z');
	{
		putchar(alphabet);
		alphabet++;
	}
	putchar('\n');
	return (0);
}
